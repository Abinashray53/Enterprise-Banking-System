apiVersion: v1
kind: PersistentVolumeClaim

metadata:

  name: redis-backup-pvc

  namespace: banking

spec:

  accessModes:

  - ReadWriteOnce

  resources:

    requests:

      storage: 20Gi

  storageClassName: gp3